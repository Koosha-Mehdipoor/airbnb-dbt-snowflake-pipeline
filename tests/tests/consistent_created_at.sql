

WITH BASE AS (
SELECT 
 R.REVIEW_DATE,
 L.CREATED_AT

FROM {{ ref('F_REVIEWS') }} AS R 
LEFT JOIN {{ ref('DIM_LISTINGS') }} AS L
ON R.LISTING_ID = L.LISTING_ID
)

SELECT * 
FROM BASE 
WHERE REVIEW_DATE < CREATED_AT
